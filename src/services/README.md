# Services (Endpoints)

This directory is for API calls and external services.

- Create a file per resource, e.g., `user.service.js`, `auth.service.js`.
- Define your endpoints here so they are reusable across the application.
