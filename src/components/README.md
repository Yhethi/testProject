# Components

This directory contains reusable UI components.

- `common/`: Small, reusable components like Buttons, Inputs, Cards.
- `layout/`: Components that define the layout structure like Headers, Footers, Sidebars.
- `features/`: Complex components specific to a feature (optional).
